// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: 'export',
// };

// module.exports = nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { unoptimized: true },
  basePath: "/Aigiri-Landing",
  output: "export",
};

export default nextConfig;
